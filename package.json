{
  "name": "zenflow-spa-be",
  "version": "1.0.0",
  "description": "ZenFlow Spa Backend — Hono on Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.1",
    "hono": "^4.4.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "tsx": "^4.22.3",
    "typescript": "^5.4.5",
    "wrangler": "^4.94.0"
  }
}
